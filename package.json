{
  "name": "portofoliorafii",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "next build --turbopack",
    "deploy": "npm run build && rm -rf docs && mkdir docs && cp -R out/* docs/ && echo rafiimalfa.com > docs/CNAME && touch docs/.nojekyll && git add -A docs && git commit -m \"chore: publish docs $(date -u +'%Y-%m-%d %H:%M')\" || true && git push origin main"
  },
  "dependencies": {
    "next": "15.5.2",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.5.2",
    "gh-pages": "^6.3.0",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}